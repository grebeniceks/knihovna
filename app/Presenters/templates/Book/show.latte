{block content}
    <h1 n:block="title">{$book->name}</h1>
    <div class="book">Author: {$book->author->name} {$book->author->surname}</div>
    <div class="book">Description: {$book->description}</div>
    <div class="book">Year: {$book->year}</div>
    <div class="book">Pages: {$book->pages}</div>
    <a class="btn btn-primary" n:href="Edit:edit $book->id">Edit</a>
    <p><a n:href="Home:default">← back to home</a></p>


{/block}
